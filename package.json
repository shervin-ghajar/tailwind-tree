{
  "name": "tailwind-tree-monorepo",
  "private": true,
  "version": "0.0.1",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "pnpm --filter=tailwind-tree build",
    "lint": "pnpm --filter=tailwind-tree lint",
    "dev": "rollup -c -w"
  }
}
